package unit4.graphicsProgram;

import acm.graphics.GCompound;
import acm.graphics.GRect;

@SuppressWarnings("serial")
public class Sky extends GCompound
{
	public Sky()
	{
		add(new GRect(0,0,0,0));
	}
}
